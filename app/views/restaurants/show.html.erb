 
<h1><%= @restaurant.name %></h1>
<%= link_to "note", new_restaurant_review_path(@restaurant)  %>
 
<% @restaurant.reviews.each do |review| %>
  <li>
  <%= review.rating %>
  <%= review.content %>
  </li>
  <%= link_to "Update", edit_restaurant_review_path(@restaurant, review), class:"btn btn-danger"%>
<% end %>
<br>
<%= link_to "Back", restaurants_path, class:"btn btn-primary" %>


